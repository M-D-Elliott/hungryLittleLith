class DashBoardMenu extends Menu {
    constructor(canvas, ctx, bgColor, posY, height) {
        super(canvas, ctx, bgColor, 0, posY, canvas.width, height);
    }

    update(clicks){
        super.update(clicks);
    }

    render(){
        super.render();
    }
}